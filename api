grep -o 'CVE-[0-9]\{4\}-[0-9]\+' file.json | sort -u > json_cves.txt
grep -o 'CVE-[0-9]\{4\}-[0-9]\+' file.json | sort -u > json_cves.txt
grep -o 'CVE-[0-9]\{4\}-[0-9]\+' file.csv | sort -u > csv_cves.txt

comm -3 csv_cves.txt json_cves.txt
Hi Noor — just to confirm my understanding: we should continue using the Container GEM feed we currently receive for container gating, and keep application gating using the App Sec GEM feed, i.e., keep both feeds separate as today. Is that correct?

