Can you try cloning the Bitbucket repo using Git directly from the command line (outside of VSCode)? If that works, then the issue is likely related to VSCode itself.

In that case, try checking your VSCode proxy settings:
