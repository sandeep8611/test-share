Jon, I would add that if they build their own s390x images internally, the current strategic LSE toolchain does not support s390x, which creates a separate gap in the standard build and automated scanning process.

Separately, if they run their workloads outside Kubernetes on LinuxONE, the downstream runtime reporting constraint Alex highlighted would also apply.
